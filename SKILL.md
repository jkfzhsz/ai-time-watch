---
name: "ai-time-watch"
description: "AI时序管理技能，为Agent提供'手表'能力。检测用户输入中的时间概念，判定过去/现在/将来时态，对将来事件执行预测型/定时型二次分流并自动调用Schedule创建定时任务。每次对话中用户语句出现时间概念时强制触发。"
---

# AI Time Watch（AI 时序管理）

你是一块"手表"，为 Agent 提供时间感知能力。在每轮对话中，**一旦用户语句中出现时间概念，你必须强制执行以下流程**。

## 核心规则

### 1. 时间信号检测

扫描用户输入，提取所有时间相关表述。时间概念包括但不限于：

- **绝对时间**："2024年"、"2026年7月"、"昨天"、"明天"、"下周一"、"去年"
- **相对时间**："刚才"、"前几天"、"过几天"、"尽快"、"稍后"
- **进行中**："现在"、"当前"、"正在"、"今天"
- **周期性**："每天"、"每周"、"每月"、"每个工作日"

### 2. 时态判定

以当前系统时间（`Today's date`）为基准，对所有提取到的时间信号逐一判定：

| 时态 | 判定标准 | 行动 |
|------|----------|------|
| **过去** | 时间点 < 当前时间 | 输出判定结果，允许 Agent 继续推理 |
| **现在** | 时间点 ≈ 当前时间（含进行中任务） | 输出判定结果，允许 Agent 继续推理 |
| **将来** | 时间点 > 当前时间 | 进入二次判定（见第4节） |

### 3. "当下"基准点与时间粒度

Agent 所处的环境提供以下时间信息：

| 信息来源 | 精度 | 示例 |
|----------|------|------|
| 系统提示 `Today's date` | **日期级**（年月日） | `2026-07-22` |
| 系统提示 `Current local timezone` | 时区标识 | `Asia/Shanghai` |
| 对话上下文时间戳 | 可推断（非显式） | 当前对话发生时的小时/分钟 |

**核心原则：以可用的最高精度为基准，无法获取则逐级降级。**

#### 3.1 时间粒度与判定规则

| 用户时间表述的粒度 | 判定方式 |
|---|---|
| **日期级**："昨天"、"明天"、"上周"、"下个月"、"明年" | 与 `Today's date` 的**日期**直接比较。例如 2026-07-22 时，"昨天"=2026-07-21（过去），"明天"=2026-07-23（将来） |
| **时刻级**："下午3点"、"明天早上9点"、"5分钟后" | 需要**当前精确时刻**（小时:分钟）作为比较基准 |
| **时刻级 + 今天**："今天下午3点" | 若当前时刻 ≤ 15:00 → 将来；若当前时刻 > 15:00 → 过去 |

#### 3.2 精确时刻的获取策略（按优先级）

1. **优先**：系统提示中是否包含当前精确时刻（如 `Current time: 2026-07-22 14:30 CST`）→ 直接使用
2. **次选**：若对话上下文中可推断当前大致时间（如系统提示中包含 `system time` 字段）→ 使用可推断的最高精度
3. **兜底**：若仅有日期级精度，对时刻级表述采用**保守策略**：

| 仅有日期精度时，遇到时刻级表述 | 默认判定 |
|---|---|
| "今天下午3点" | 无法确定是否已过 → **默认视为将来**，并标注 `precision: "date_only"` |
| "5分钟后" | 无法确定 → **默认视为将来** |
| "现在" / "当前" / "正在" | 日期级精度即可判定 → **现在** |

#### 3.3 输出中标注精度

当判定依赖的基准精度不足时，在输出中增加 `reference_precision` 字段：

```json
{
  "reference_precision": "datetime | date_only",
  "precision_note": "仅获取到日期级精度(2026-07-22)，时刻级表述'今天下午3点'默认视为将来"
}
```

**规则**：
- 当 `reference_precision = "date_only"` 且用户表述为时刻级时，始终**倾向于将来**（宁可误判为将来走定时流程，也不可误判为过去而遗漏任务）
- 此保守策略确保：即使手表精度不够，也不会漏掉需要定时调度的任务

### 4. 将来事件的二次判定

**硬约束：对于将来事件，绝对不发起 WebSearch 或 WebFetch 获取未来信息。未来不存在可搜索的事实数据。**

对每个将来事件，判断其属于以下哪种类型：

#### 预测型（Predict）
用户需要基于**现有数据**（过去/现在的信息）推测未来趋势或结果。

行动：输出判定结果，允许 Agent 继续推理（搜索过去/现在的数据，做推演分析）。

**示例**：
- "根据最近三个月的销售数据，预测下季度趋势"
- "明年这个行业会怎么样？"
- "如果按这个增速，年底能到多少用户？"
- "下周股市会涨吗？"（基于当前数据做技术分析）
- "过几天这个项目能完成吗？"（基于当前进度估算）

#### 定时型（Schedule）
用户需要在**将来的特定时间点或时间段**获取准确结论或执行具体任务。

行动：**调用 `Schedule` 工具创建定时任务**，将执行时机推迟到目标时间。

**示例**：
- "下周一早上9点帮我整理上周的销售周报"
- "每天下午5点帮我拉取当日收盘数据"
- "明天早上提醒我开会"
- "每周五生成项目进度报告"
- "每月1号汇总上月数据"

### 5. 模糊时间策略

当时间表述模糊时，采用保守策略：

| 模糊表述 | 默认策略 | 原因 |
|----------|----------|------|
| "过几天" | 将来-预测型 | 无精确时间点，无法创建定时任务 |
| "尽快" | 将来-预测型 | 同上 |
| "稍后" | 将来-预测型 | 同上 |
| "不久的将来" | 将来-预测型 | 同上 |
| 无法判断预测/定时 | **倾向于将来-定时型** | 宁可多问一次，不可漏掉定时需求 |

## 输出格式

每次检测到时间概念后，输出以下结构化判定结果：

```json
{
  "tense": "past | present | future",
  "time_expressions": ["提取的时间表述列表"],
  "action": "reason | predict | schedule",
  "reference_precision": "datetime | date_only",
  "precision_note": null,
  "schedule_suggestion": {
    "should_schedule": false,
    "cron_expression": null,
    "task_description": null,
    "reasoning": "判定理由"
  }
}
```

**字段说明**：
- `tense`: 主时态（如果存在多个不同时态，取最需要注意的 → 将来 > 现在 > 过去）
- `time_expressions`: 从用户输入中提取的所有时间表述
- `action`:
  - `reason`: 过去/现在事件，直接继续推理
  - `predict`: 将来-预测型，基于现有数据继续推理
  - `schedule`: 将来-定时型，调用 Schedule 创建定时任务
- `reference_precision`: 判定使用的基准精度
  - `datetime`: 获取到了精确到时刻的系统时间
  - `date_only`: 仅有日期级精度，时刻级表述默认保守处理
- `precision_note`: 当 `reference_precision = "date_only"` 且涉及时刻级表述时，说明保守处理的原因；否则为 null
- `schedule_suggestion`: 仅当 `action = "schedule"` 时有值

## 不触发条件

如果用户输入中**不包含任何时间概念**，本 Skill 静默跳过，不干预 Agent 正常流程。

## 多时间概念处理

如果用户输入中包含多个时间概念，按以下优先级处理：

1. 若存在将来-定时型 → 优先处理定时任务，其他时间概念作为上下文
2. 若存在将来-预测型（无定时型）→ 以预测型为主
3. 若仅有过去/现在 → 全部输出，Agent 按需消费

## 降级策略

| 异常情况 | 处理方式 |
|----------|----------|
| Schedule 工具不可用 | 仅输出判定结果，标注"定时任务创建失败：Schedule 工具不可用" |
| cron 表达式无法表达用户需求（如"第三周周五"） | 告知用户 cron 限制，建议最接近的替代 cron 表达式 |
| 时区信息缺失 | 默认使用 `Asia/Shanghai` |

## 示例

### 示例 1：过去事件
> 用户："帮我回顾一下上周的销售数据"

```json
{
  "tense": "past",
  "time_expressions": ["上周"],
  "action": "reason",
  "schedule_suggestion": null
}
```

### 示例 2：现在事件
> 用户："现在系统在线用户有多少？"

```json
{
  "tense": "present",
  "time_expressions": ["现在"],
  "action": "reason",
  "schedule_suggestion": null
}
```

### 示例 3：将来-预测型
> 用户："明年市场规模会多大？"

```json
{
  "tense": "future",
  "time_expressions": ["明年"],
  "action": "predict",
  "schedule_suggestion": null
}
```
→ 注意：Agent 搜索的是**当前**市场数据和趋势报告，而非搜索"明年"的信息。

### 示例 4：将来-定时型
> 用户："每天下午5点帮我拉取当日收盘数据"

```json
{
  "tense": "future",
  "time_expressions": ["每天下午5点"],
  "action": "schedule",
  "schedule_suggestion": {
    "should_schedule": true,
    "cron_expression": "0 17 * * 1-5",
    "task_description": "拉取当日收盘数据",
    "reasoning": "用户需要在每个工作日下午5点获取当日数据，属于定时型任务"
  }
}
```

### 示例 5：多时间概念
> 用户："回顾上周的销售情况，预测下周的趋势"

```json
{
  "tense": "future",
  "time_expressions": ["上周", "下周"],
  "action": "predict",
  "schedule_suggestion": null
}
```
→ 注意："上周"（过去）作为"下周"（将来-预测型）的上下文数据，以预测型为主。

### 示例 6：精度不足 — 仅有日期级基准
> 假设当前系统仅提供 `Today's date: 2026-07-22`，无精确时刻
> 用户："今天下午3点帮我查一下账户余额"

```json
{
  "tense": "future",
  "time_expressions": ["今天下午3点"],
  "action": "schedule",
  "reference_precision": "date_only",
  "precision_note": "仅获取到日期级精度(2026-07-22)，无法判断'下午3点'是否已过，按保守策略默认视为将来",
  "schedule_suggestion": {
    "should_schedule": true,
    "cron_expression": "0 15 22 7 *",
    "task_description": "查询账户余额",
    "reasoning": "时刻级表述'今天下午3点'在仅有日期精度时默认视为将来-定时型"
  }
}
```
→ 注意：`reference_precision: "date_only"` 告知下游 Agent 此判定基于保守假设。若实际已过 15:00，Agent 可选择直接执行而非等待定时触发。